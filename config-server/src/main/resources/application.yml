server:
  port: 8888

spring:
  application:
    name: config-server
  cloud:
    config:
      server:
        git:
          uri: file:///C:/Users/User-PC/IGL5-G4-projet-microservices/config-repo
          default-label: main
          clone-on-start: true
          force-pull: true