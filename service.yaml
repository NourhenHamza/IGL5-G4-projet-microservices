apiVersion: v1
kind: Service
metadata:
  name: springboot-service
spec:
  type: NodePort
  selector:
    app: springboot-app   # 🔹 doit correspondre au label du Deployment
  ports:
    - protocol: TCP
      port: 8082          # 🔹 port interne du service
      targetPort: 8082    # 🔹 port du conteneur
      nodePort: 30000     # 🔹 port exposé sur le nœud Kubernetes (30000-32767)
